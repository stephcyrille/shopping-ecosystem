/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // React strict mode for development warnings
  swcMinify: true, // Enables faster builds with SWC
};

export default nextConfig;
